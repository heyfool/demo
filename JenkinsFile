pipeline{
    agent {
        label 'testLabel'
    }
    stages {
        stage('utility tool method') {
            steps{
                script {
                    def files = findFiles(glob: '**/*.log')
                    echo files[0].name
                }
            }
        }
    }
}