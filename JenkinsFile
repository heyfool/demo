pipeline{
    agent {
        label 'testLabel'
        customWorkspace "${env.JOB_NAME}"
    }
    stages {
        stage('utility tool method') {
            steps{
                script {
                    def files = findFiles(glob: '**/*.log')
                    echo files[0].name
                }
            }
        }
    }
}